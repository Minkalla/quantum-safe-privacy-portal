name: CI - Dual Backend Validation v3

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/ci-cd-validation-adjusted-v3.yml'
  workflow_dispatch:

jobs:
  fake-pass:
    runs-on: ubuntu-latest
    steps:
      - name: Immediate pass
        run: echo "✅ Success"
